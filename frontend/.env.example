# Copy to .env.local for local dev overrides
# The vite proxy handles /api routing in dev, so this is only needed if you
# want to point at a remote backend instead.
VITE_API_URL=http://localhost:3001
